Create exactly one freestanding airport passenger information directory kiosk as a single 2D game prop.

Use the visible EverrichRPG asset style reference board, the legacy narrow digital map kiosk, and the warm-ivory checkout counter just shown as visual references. Preserve the project's modern detailed pixel-inspired airport RPG language: warm ivory structural shell, navy lower body, muted gold trim, cyan screen accents, soft navy or warm-charcoal outline, crisp intentional pixel clusters, medium detail, and soft upper-left lighting.

The new object replaces the legacy directory kiosk. Make it a wider, readable upright directory terminal rather than a thin pillar: approximately 2:3 overall silhouette, designed to display at 48x72 game pixels, bottom-center anchor, with a 32x16 ground footprint. Use a strict front-facing top-down RPG object view: the kiosk front plane faces the camera squarely with zero left/right yaw, zero diagonal rotation, and no isometric slant. The left and right outer edges must be vertical, parallel, equally visible, and mirror-balanced. Every shelf, trim line, screen edge, and base line must remain horizontal. Show only a shallow, centered top surface caused by the slightly elevated camera; do not expose either side panel more than the other. Keep it vertically centered and fully contained with generous margin. The screen may contain abstract colored route blocks and map lines only—no readable letters, words, numbers, logos, symbols, labels, or UI text.

Exactly one object, no alternate versions, no sprite sheet cells, no floor, no environment, no cast shadow outside the object silhouette, no detached parts, no glow outside the silhouette. Background must be 100% solid flat #FF00FF magenta with no gradient, texture, border, frame, or shadow. Do not use smooth vector rendering, photorealism, strong isometric rotation, heavy black outlines, noisy dithering, or retro 16-bit chunkiness.
